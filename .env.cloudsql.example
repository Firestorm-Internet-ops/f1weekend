# Google Cloud SQL connection settings
# Copy this file to .env.cloudsql and fill in your values.
# .env.cloudsql is gitignored — never commit it.

# Cloud SQL instance connection name (from GCP Console → SQL → your instance → Connection name)
# Format: project-id:region:instance-name
CLOUDSQL_INSTANCE=your-project:your-region:your-instance

# Cloud SQL user with write access to the target database
CLOUDSQL_USER=root

# Password for CLOUDSQL_USER (leave empty if using IAM auth)
CLOUDSQL_PASSWORD=

# Target database name on Cloud SQL (will be dropped and recreated on each run)
CLOUDSQL_DB=f1weekend

# Local port for cloud-sql-proxy (3307 avoids conflict with local MySQL on 3306)
CLOUDSQL_PROXY_PORT=3307
